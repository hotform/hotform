{
  "devDependencies": {
    "lerna": "^6.4.1",
    "rimraf": "^4.1.2",
    "typescript": "^4.9.5"
  },
  "private": true,
  "scripts": {
    "build:clean": "rimraf ./packages/*/build",
    "build:types": "tsc -p ./packages/hotform-types/tsconfig.json",
    "build": "yarn build:clean && yarn build:types",
    "lerna": "lerna"
  },
  "workspaces": [
    "packages/*"
  ]
}
